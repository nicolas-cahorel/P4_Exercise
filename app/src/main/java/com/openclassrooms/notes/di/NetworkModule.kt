package com.openclassrooms.notes.di

/**
 * This module is responsible for configuring network-related dependencies.
 *
 * Although this module is not currently used in the project, it is intended to provide
 * a centralized place for defining network services such as API clients, Retrofit instances,
 * and other related components.
 *
 * Potential usage:
 * - Define Retrofit instances for making HTTP requests.
 * - Configure OkHttpClient for network operations.
 * - Provide API service interfaces.
 */
class NetworkModule {
    // Add your network methods here, not used in the project
}